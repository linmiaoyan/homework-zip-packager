# Python 缓存
__pycache__/
*.pyc
*.pyo

# macOS
.DS_Store

# WorkBuddy 项目内部数据（不对外）
.workbuddy/

# 本机专属启动器（含绝对路径，不可移植）
作业收集服务.app/

# 构建产物（deployed/ 与 收集/ 按需求入库）
build/
dist/
*.spec

# 部署产物中的 Windows 可执行文件（体积大，按需本地生成，不入库）
# 覆盖 deployed/<slug>/package/*.exe 等任何位置生成的 exe
deployed/**/*.exe
